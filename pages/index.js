//Components from MUI
import Box from '@mui/material/Box';

const index = () => {
  return (
    <Box
      sx={{
        height: '100vh',
        bgcolor: 'primary.main'
      }}
    >

    </Box>
  );
};

export default index;
